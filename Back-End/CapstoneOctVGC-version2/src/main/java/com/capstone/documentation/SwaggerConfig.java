package com.capstone.documentation;

public class SwaggerConfig {
	/*
	@Bean
	public Docket api() {
		return new Docket(DocumentationType.SWAGGER_2)
						.select()
						.apis(RequestHandlerSelectors.basePackage("org.springframework.boot"))
						.paths(PathSelectors.any())
						.build();
	}
	
	public void addResourceHandler(ResourceHandlerRegistry registry) {
		registry.addResourceHandler("swagger-ui.html").addResourceLocations("classpath:/META-INF/resources");
		registry.addResourceHandler("/webjars/**").addResourceLocations("classpath:/META-INF/resources/webjars");
	}
	*/
	/*
	private ApiInfo apiInfo() {
		Contact contact = new Contact("Veronica Gutierrez Camacho", "-", "veronica.gutie@hcl.com");
		return new ApiInfoBuilder()
					.title("Capstone: E-Commerce Project")
					.description("Documentation for Oct Capstone")
					.version("1.0.0")
					.license("Apache 2.0")
					.licenseUrl("http://www.apache.org/licenses/LICENSE-2.0")
					.contact(contact)
					.build();
	}
	*/
}
